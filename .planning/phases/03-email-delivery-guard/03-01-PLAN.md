---
phase: 03-email-delivery-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps-script/Code.gs
autonomous: false
requirements:
  - EMAIL-01
  - EMAIL-02
  - EMAIL-03
  - EMAIL-04
  - EMAIL-05
  - GUARD-01
  - GUARD-02

must_haves:
  truths:
    - "Parent receives one HTML email with warm church-friendly tone — no 'print or show' instructions"
    - "Each child's PDF attachment is named VBS2026_Pass_[ChildName].pdf"
    - "After successful send, Status cell reads 'Approved — Email Sent'"
    - "Re-editing Status to 'Approved' on a sent row does nothing (guard already blocks via trigger condition)"
    - "If PDF generation fails, Status is set to 'Error — Email Failed' and nothing is sent"
    - "If email send fails, Status is set to 'Error — Email Failed' and Logger.log records the error"
  artifacts:
    - path: "apps-script/Code.gs"
      provides: "Corrected generatePassPdf() and sendApprovalEmail() functions"
      contains: "VBS2026_Pass_"
  key_links:
    - from: "generatePassPdf()"
      to: "email attachment filename"
      via: ".setName('VBS2026_Pass_' + safeName + '.pdf')"
      pattern: "VBS2026_Pass_"
    - from: "sendApprovalEmail() catch blocks"
      to: "Status cell"
      via: "sheet.getRange(row, STATUS_COLUMN).setValue('Error — Email Failed')"
      pattern: "Error — Email Failed"
    - from: "sendApprovalEmail()"
      to: "GmailApp.sendEmail()"
      via: "htmlBody option with inline-CSS HTML string"
      pattern: "htmlBody"
---

<objective>
Fix four concrete gaps in the existing Phase 3 email implementation in Code.gs so that the parent
receives exactly one branded HTML email with correctly-named PDF attachments, and failures are
correctly surfaced in the Status cell.

Purpose: The existing sendApprovalEmail() and generatePassPdf() functions are wired correctly but
have four gaps against the locked CONTEXT.md decisions — plain text instead of HTML, wrong blob
filename pattern, forbidden "Print and bring" copy, and a catch block that doesn't write
"Error — Email Failed". This plan closes all four gaps surgically.

Output: apps-script/Code.gs with corrected generatePassPdf() and sendApprovalEmail() functions.
No other functions are modified. No new files.
</objective>

<execution_context>
@/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-email-delivery-guard/03-CONTEXT.md
@.planning/phases/03-email-delivery-guard/03-RESEARCH.md
@apps-script/Code.gs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix generatePassPdf() — rename blob to VBS2026_Pass_[ChildName].pdf with filename sanitization</name>
  <files>apps-script/Code.gs</files>
  <action>
In generatePassPdf() (currently around line 624), make two changes only — do not touch anything else in this function:

1. Add filename sanitization at the top of the function body (first line after the opening brace):
   var safeName = childName.replace(/[\/\\:*?"<>|]/g, '_');

2. Change the .setName() call from:
   .setName(childName + ' - VBS Pass 2026.pdf')
   to:
   .setName('VBS2026_Pass_' + safeName + '.pdf')

The function signature stays the same (no new parameters). The safeName variable is derived inline from childName. The temp HTML file creation and the try/finally around setTrashed remain unchanged.

Per CONTEXT.md locked decision: attachment pattern is VBS2026_Pass_[ChildName].pdf — no age in filename, no sorting.
Per RESEARCH.md Pattern 3 and filename sanitization: replace /[\/\\:*?"<>|]/g with '_'.
  </action>
  <verify>
    <automated>grep -n "VBS2026_Pass_" /Users/User/Documents/vbs/apps-script/Code.gs</automated>
    <manual>Confirm output shows the .setName() line inside generatePassPdf() with the VBS2026_Pass_ prefix. Also confirm the safeName sanitization line is present just before or near it.</manual>
  </verify>
  <done>generatePassPdf() names every PDF blob as VBS2026_Pass_[safeName].pdf where safeName has OS-invalid characters replaced with underscores. The function signature is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite sendApprovalEmail() — two-stage error handling, HTML body, remove forbidden copy</name>
  <files>apps-script/Code.gs</files>
  <action>
Replace the entire sendApprovalEmail() function body (lines ~651-695) with the following implementation. Do not change the function signature.

The new implementation must:

1. TWO-STAGE ERROR HANDLING — split the single try/catch into two separate try/catch blocks:
   - Stage 1 try/catch: call generatePassesForRegistration(data) to build passPdfs. On catch: Logger.log('sendApprovalEmail PDF ERROR row ' + row + ': ' + pdfErr.message), set Status cell to 'Error — Email Failed', return.
   - Stage 2 try/catch: build subject/body/htmlBody, call GmailApp.sendEmail(), set Status to 'Approved — Email Sent' on success. On catch: Logger.log('sendApprovalEmail MAIL ERROR row ' + row + ': ' + mailErr.message), set Status to 'Error — Email Failed'.

2. PLAIN-TEXT BODY (body parameter — required GAS fallback) — rewrite to match the HTML content but as plain text, with no "Print and bring" copy and no pass-handling instructions at all. Include: greeting, confirmation, reference ID, children list, event dates/times, picnic line (conditional on data.picnicConsent === 'Yes'), venue/address, warm closing, footer contact info (ebethelchurch@gmail.com and phone 9945028989).

3. HTML BODY (htmlBody option) — build as an inline-CSS HTML string using a helper function buildEmailHtml(data). All CSS must be inline style attributes — no style blocks, no link tags (Gmail strips them). Structure:
   - Dark green header (#2d5a27) with church name and "VBS 2026 — Jungle Safari"
   - Greeting: "Dear [parentName],"
   - "We're so excited to have your children at VBS 2026 — Jungle Safari! Your registration has been approved."
   - Reference ID box (light grey background, border)
   - "Registered Children:" unordered list: "1. [name] (Age [age])" per child
   - Event Details table: Dates (May 11-15, 2026, Monday to Friday), Time (10:00 AM - 1:00 PM), Day 5 row only if data.picnicConsent === 'Yes' (Full Day Picnic, May 15, 2026), Venue (El Bethel AG International Church), Address (CHURCH_ADDRESS constant)
   - Warm closing: "God bless you! We look forward to a wonderful week with your children."
   - Signature: "Warm regards, El Bethel VBS 2026 Team"
   - Footer (light grey background): church name, CHURCH_ADDRESS, "Email: ebethelchurch@gmail.com | Phone: 9945028989"
   - DO NOT include any instructions about what to do with the passes (no "print", no "bring", no "show on phone")

4. GMAILAPP CALL options object must include: htmlBody, attachments: passPdfs, name: 'El Bethel VBS 2026', from: CHURCH_EMAIL.

5. Add buildEmailHtml(data) as a new function immediately after sendApprovalEmail() in the file. This function builds and returns the HTML string. It uses data.parentName, data.submissionId, data.kids (array of {name, age}), data.picnicConsent, CHURCH_ADDRESS constant.

The guard logic (silent exit on duplicate) requires no change — the trigger already only fires when e.value === 'Approved', and "Approved — Email Sent" never equals "Approved". No check is needed inside sendApprovalEmail() itself.

Reference the code skeleton in RESEARCH.md section "Complete Revised sendApprovalEmail() Skeleton" and "HTML Email Body Template Pattern" for exact structure.
  </action>
  <verify>
    <automated>grep -n "htmlBody\|Error — Email Failed\|Approved — Email Sent\|Print and bring\|buildEmailHtml" /Users/User/Documents/vbs/apps-script/Code.gs</automated>
    <manual>Confirm: (1) htmlBody appears in GmailApp.sendEmail() options, (2) "Error — Email Failed" appears in both catch blocks, (3) "Approved — Email Sent" appears in the success branch, (4) "Print and bring" does NOT appear anywhere in the file, (5) buildEmailHtml is defined as a function.</manual>
  </verify>
  <done>sendApprovalEmail() uses two-stage error handling with Status writes in both catch blocks. GmailApp.sendEmail() is called with htmlBody. Plain-text body has no forbidden copy. buildEmailHtml(data) exists and produces inline-CSS HTML with footer containing ebethelchurch@gmail.com and 9945028989. "Print and bring" does not appear anywhere in Code.gs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live deployment test — verify HTML email and attachment names end-to-end</name>
  <files>apps-script/Code.gs</files>
  <action>
Paste the updated apps-script/Code.gs into the Google Apps Script editor and run the live end-to-end test described in how-to-verify below. No code changes in this task — this is a human verification step after the automated tasks complete.
  </action>
  <verify>
    <automated>MISSING — live GAS execution cannot be automated; human must test in Apps Script editor</automated>
    <manual>
    1. Copy the contents of apps-script/Code.gs into the Google Apps Script editor (replacing existing content)
    2. Run installTrigger() once from the editor (if trigger not already installed — safe to re-run)
    3. In the Registrations sheet, set a test row's Status column to "Approved"
    4. Check the parent email address — confirm one HTML email arrived with:
       - Subject: "VBS 2026 Registration Approved — [Parent Name]"
       - Green header with "El Bethel AG International Church" and "VBS 2026 — Jungle Safari"
       - Greeting and warm tone — no pass-handling instructions anywhere
       - Reference ID, children list, event dates, footer with ebethelchurch@gmail.com and 9945028989
       - PDF attachments named VBS2026_Pass_[ChildName].pdf (one per child)
    5. Check the sheet — Status cell should now read "Approved — Email Sent"
    6. Re-set the same row's Status to "Approved" — confirm NO second email arrives
    7. Check Apps Script Executions log — confirm success Logger.log entry is present
    </manual>
  </verify>
  <done>One HTML email received with correct branding and correctly-named PDF attachments. Status reads "Approved — Email Sent". Re-approving the same row sends no second email.</done>
</task>

</tasks>

<verification>
After Tasks 1 and 2 complete, run:

grep -n "VBS2026_Pass_\|htmlBody\|Error — Email Failed\|Approved — Email Sent\|Print and bring\|buildEmailHtml\|safeName" /Users/User/Documents/vbs/apps-script/Code.gs

Expected: VBS2026_Pass_ present in generatePassPdf(), htmlBody present in GmailApp call, "Error — Email Failed" in two catch blocks, "Approved — Email Sent" in success branch, "Print and bring" NOT found (grep returns nothing for that pattern), buildEmailHtml defined, safeName used in generatePassPdf().
</verification>

<success_criteria>
1. apps-script/Code.gs generatePassPdf() names blobs as VBS2026_Pass_[safeName].pdf
2. apps-script/Code.gs sendApprovalEmail() sends with htmlBody option
3. Both catch blocks (PDF stage and mail stage) write "Error — Email Failed" to Status cell
4. No occurrence of "Print and bring" in Code.gs
5. buildEmailHtml(data) function exists with inline CSS, footer contains ebethelchurch@gmail.com and 9945028989
6. Live test: HTML email arrives with correct attachments, Status reads "Approved — Email Sent", re-approval does not trigger second send
</success_criteria>

<output>
After completion, create `.planning/phases/03-email-delivery-guard/03-01-SUMMARY.md` using the summary template.
</output>
